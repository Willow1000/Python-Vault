Potential improvements:
    Nesting the choices within 'while True' loops to minimise chances of errors as a result odd inputs
    Decentralising the files and Backup folder in different folder to minimise chances of easy access incase of an attack
    Creating a bash script that makes the folder containing the files to dissappear upon successful code execution (attrib +h +s +r foldername) ~ (attrib -h -s -r foldername) to bring it back
    Use Cipher command (cipher /E) in CMD to secure the folders containing the files and Back up folder